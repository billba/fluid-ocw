{
  "name": "fluid-ocw",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "@fluidframework/azure-client": "^0.59.2001",
    "@fluidframework/sequence": "^0.59.3000",
    "@fluidframework/test-client-utils": "^0.59.2001",
    "dotenv": "^16.0.1",
    "fluid-framework": "^0.59.2001"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "typescript": "^4.6.4"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
